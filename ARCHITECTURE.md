# Architecture — Transport Needs Assessment Tool

> Quick reference for Claude. See `CLAUDE.md` for conventions & commands.

## Folder Map
```
src/
  app/                          # Next.js App Router pages
    page.tsx                    # Home dashboard
    layout.tsx                  # Root layout (Header + Sidebar)
    globals.css
    admin/
      page.tsx                  # Admin panel: cache stats, config, refresh logs
    api/
      live-data/[areaId]/
        route.ts                # Live data API — reads SQLite, returns merged live+sample JSON
      admin/refresh/
        route.ts                # POST to trigger seed; GET for status (requires ADMIN_KEY)
      abs/[lgaId]/
        route.ts                # Legacy stub (kept for backwards compat)
    demographics/page.tsx
    economy/page.tsx
    education/page.tsx
    growth/page.tsx
    housing/page.tsx
    transport/page.tsx
    compare/page.tsx
    report/page.tsx
    upload/page.tsx             # Excel upload (NSW Planning XLSX)

  components/
    layout/
      Header.tsx                # Global nav bar (AreaSelector + YearSelector only — no ABS toggle)
      Sidebar.tsx
      AreaSelector.tsx          # Populates from SAMPLE_AREAS
      YearSelector.tsx
    charts/
      BarChart.tsx · LineChart.tsx · PieChart.tsx
      PopulationPyramid.tsx · StackedBar.tsx
      ChartWrapper.tsx          # Responsive container
    maps/
      ChoroplethMap.tsx         # Entry point (dynamic import wrapper)
      ChoroplethMapDirect.tsx
      ChoroplethMapInner.tsx    # Actual Leaflet map
      MapLegend.tsx · MapRenderer.tsx · SimpleMap.tsx
    tables/
      DataTable.tsx             # @tanstack/react-table wrapper
    ui/
      StatCard.tsx
      DataSourceBadge.tsx       # Shows "ABS Census 2021 · live" or "Sample data" per field
    admin/
      ProjectionUploadPanel.tsx # Drag-drop XLSX upload UI

  hooks/
    useLiveData.ts              # SWR hook → /api/live-data/[areaId]; returns all domains

  lib/
    abs-fetchers.ts             # ABS SDMX-ML (XML) fetchers; also exports LGA_CODE_MAP
    db.ts                       # better-sqlite3 wrapper — schema init, upserts, queries
    seed-runner.ts              # In-process seed logic (used by admin API route)
    excel-parser.ts             # NSW Planning XLSX → NSWPopulationProjection[]
    tfnsw-api.ts                # TfNSW API scaffold (future live calls)
    utils.ts                    # formatNumber/Percent/Currency, CHART_COLORS, YEARS
    data/
      live-data.ts              # Live/sample merge layer — reads SQLite, falls back field-by-field
      lga-mapping.ts            # Internal ID ↔ official name (legacy — LGA_CODE_MAP is authoritative)
      sample-areas.ts           # SAMPLE_AREAS array (37 LGAs)
      sample-data.ts            # Mock data functions getXxxData(areaId, year)
      demographic-indicators.ts
      nsw-projections-data.ts   # Bundled NSW DPE population projections (generated by parse:population)
      nsw-employment-projections.ts  # Bundled TZP24 employment projections (generated by parse:employment)
      tfnsw-transport.ts        # Bundled TfNSW mode share / commute data
    types/
      projections.ts            # NSWPopulationProjection, ProjectionMetadata

  store/
    index.ts                    # useAppStore: selectedArea, selectedYear
    projectionStore.ts          # useProjectionStore: uploaded NSW Excel projections → localStorage

  types/
    index.ts                    # Shared TS types (Area, DataPoint, etc.)

scripts/
  seed-db.ts                    # CLI seed script (tsx scripts/seed-db.ts [--static|--abs|--lga id])

data/
  cache.db                      # SQLite database (created on first seed run, gitignored)

public/
  geo/lga-greater-sydney.json   # GeoJSON boundaries (large — don't read unless editing maps)

parse-projections.js            # Generates nsw-projections-data.ts from XLSX source
parse-employment.js             # Generates nsw-employment-projections.ts from CSV source
```

## Key Data Flows

### Live Data (primary path)
```
Page component
  → useLiveData(areaId, year)        [src/hooks/useLiveData.ts]
    → SWR → GET /api/live-data/[areaId]
      → getLiveDemographicsData()    [src/lib/data/live-data.ts]
         getABSCacheEntry(lgaCode, 'G01')   ← SQLite abs_cache
         getABSCacheEntry(lgaCode, 'G02')   ← SQLite abs_cache
         ... (G33, G36, G51, G46, G49, SEIFA, LABOUR, ERP)
         getNSWProjection(lgaName)          ← SQLite nsw_projections
         getTransportStaticForLGA(lgaName)  ← SQLite transport_static
         field-by-field fallback to sample-data.ts if SQLite row missing
      → { data: XxxData, meta: DataMeta }
         meta.liveFields / meta.sampleFields show which fields are live
  → Page renders DataSourceBadge with meta
```

### Seeding the SQLite Cache
```
CLI: npm run seed [--static] [--abs] [--lga id]
  → scripts/seed-db.ts
    --static: reads nsw-projections-data.ts + nsw-employment-projections.ts + tfnsw-transport.ts
              → upserts into nsw_projections + transport_static tables
    --abs:    iterates LGA_CODE_MAP, calls fetchAllABSDataForLGA(lgaCode) for each
              → fetchG01, fetchG02, fetchG33, fetchG36, fetchG51, fetchG46, fetchG49,
                fetchSEIFA, fetchLabour, fetchERP  (10 SDMX-ML XML requests per LGA)
              → upserts into abs_cache table

Admin API: POST /api/admin/refresh { mode, lgaId }  (requires ADMIN_KEY header)
  → src/lib/seed-runner.ts  (same logic, in-process)
```

### NSW Excel Projections (manual upload path)
```
/upload → ProjectionUploadPanel
  → parseNSWProjectionsExcel(arrayBuffer)   [src/lib/excel-parser.ts]
    → match LGA names via LGA_MAPPING
  → useProjectionStore.setProjections()
    → localStorage.nsw_population_projections
  → available in any page via useProjectionStore()
  (Also upserted into nsw_projections SQLite table for use by live-data layer)
```

## SQLite Schema (`data/cache.db`)
| Table | Unique key | Purpose |
|---|---|---|
| `abs_cache` | `(lga_code, dataset)` | Parsed ABS Census / ERP data per LGA |
| `nsw_projections` | `(lga_name, projection_type)` | Population & employment projections |
| `transport_static` | `(lga_name, year)` | TfNSW mode share / commute metrics |
| `tfnsw_cache` | `(lga_code, dataset)` | Reserved for future live TfNSW API |
| `app_config` | `key` | Key-value store (e.g. `abs_last_seeded`) |
| `refresh_log` | `id` | Audit log of seed runs |

## ABS Fetcher Details (`src/lib/abs-fetchers.ts`)
- **Format**: SDMX-ML (XML) — `Accept: application/xml`
- **Auth**: None required; needs `User-Agent: Mozilla/5.0` (CloudFront blocks bot UAs)
- **Base URL**: `https://data.api.abs.gov.au/rest/data`

| Function | Dataset ID | Dimensions used |
|---|---|---|
| `fetchG01Population` | C21_G01_LGA | SEXP |
| `fetchG02Medians` | C21_G02_LGA | MEDAVG (1=age, 2=hh_income, 3=mortgage, 6=rent, 8=avg_hhd_size) |
| `fetchG33DwellingStructure` | C21_G36_LGA | DWTSTRD (11=sep house, 2=semi, 3=flat, 9=other) |
| `fetchG36TenureType` | C21_G37_LGA | TENLLD (1=owned, 2=mortgage, 3=rented) |
| `fetchG51Industry` | C21_G53_LGA | INDP (ANZSIC A–S letter codes) |
| `fetchG46SchoolCompletion` | C21_G16_LGA | HSCP (1=Yr12 … 6=none) |
| `fetchG49Qualifications` | C21_G49_LGA | QALLP (1=postgrad, 3=bach, 51=adv_dip, 4=cert3/4) |
| `fetchSEIFA` | ABS_SEIFA2021_LGA | SEIFAINDEXTYPE × SEIFA_MEASURE=SCORE |
| `fetchLabour` | C21_G46_LGA | LFSP (1=FT emp, 2=PT emp, 4=unemp, 5+=NILF) |
| `fetchERP` | ABS_ANNUAL_ERP_LGA2021 | LGA_2021 × SEX_ABS=3 × AGE=TOT |
| G55 (journey-to-work) | — | **No SDMX endpoint at LGA level — stubbed null** |

## LGA Coverage
128 NSW LGAs in `LGA_CODE_MAP` (all 10 NSW regions) + `benchmark_gsy` (1GSYD) + `benchmark_nsw` (1NSW).
Regions: Greater Sydney (33) · Hunter (9) · Central Coast (1) · Illawarra-Shoalhaven (5) · South East & Tablelands (8) · New England & North West (12) · North Coast (13) · Central West & Orana (19) · Riverina-Murray (19) · Far West (8)
Key codes: `lga_sydney→17200` `lga_parramatta→16260` `lga_blacktown→10750` `lga_newcastle→15900` `lga_wollongong→18450`
All 37 Greater Sydney LGAs have been seeded with 10/10 ABS datasets (0 errors as of 2026-03-29). Regional LGAs pending `npm run seed:abs`.
